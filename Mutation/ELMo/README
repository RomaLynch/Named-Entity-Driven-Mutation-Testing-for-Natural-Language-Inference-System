# We need to run First_Round_Inference_ELMo.py to extract neutral sentences from three datastes.
# Then we can run ELMoMutation.py. We load Cities.txt and ORG.txt for our mutation: Cities.txt is used for LOCATION mutation, and ORG.txt is used for ORGANIZATION mutation.
